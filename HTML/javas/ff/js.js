const btn = document.getElementById(' toggle-btn');
const div = document.querySelector.Selector('.element');

function toggleDivVisibility () {
    div.classlist.toggle('hide')
}

btn.addEventlistener ('click', toggleDivVisibility);